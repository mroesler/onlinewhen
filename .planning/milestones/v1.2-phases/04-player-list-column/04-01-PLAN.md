---
phase: 04-player-list-column
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - UI/Window.lua
  - UI/TabPlayers.lua
autonomous: true
requirements:
  - LIST-04
must_haves:
  truths:
    - "The main window is 950px wide on screen"
    - "All column X positions and widths include the new Activity column slot"
    - "CONTENT_W reflects the new window width minus insets"
  artifacts:
    - path: "UI/Window.lua"
      provides: "Updated WINDOW_W constant"
      contains: "local WINDOW_W  = 950"
    - path: "UI/TabPlayers.lua"
      provides: "Updated CONTENT_W and COL_X/COL_W with activity column"
      contains: "activity"
  key_links:
    - from: "UI/Window.lua"
      to: "UI/TabPlayers.lua"
      via: "WINDOW_W determines CONTENT_W (WINDOW_W - INSET*2)"
      pattern: "CONTENT_W.*=.*938"
---

<objective>
Widen the main window from 800 to 950 pixels and update all column layout constants in TabPlayers.lua to insert an Activity column after Spec.

Purpose: LIST-04 requires the window to be wide enough for the new Activity column. This plan establishes the layout foundation that Plans 02 and 03 build upon.
Output: Updated WINDOW_W, CONTENT_W, COL_X, and COL_W constants.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-player-list-column/04-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update WINDOW_W in UI/Window.lua</name>
  <files>UI/Window.lua</files>
  <read_first>UI/Window.lua</read_first>
  <action>
Change line 7 of UI/Window.lua:
- FROM: `local WINDOW_W  = 800`
- TO:   `local WINDOW_W  = 950`

No other changes in this file. WINDOW_H stays at 536. INSET stays at 6.
Per D-01: WINDOW_W increases from 800 to 950.
  </action>
  <verify>
    <automated>grep -n "local WINDOW_W" UI/Window.lua</automated>
  </verify>
  <acceptance_criteria>
- UI/Window.lua line 7 contains exactly `local WINDOW_W  = 950`
- No other lines in UI/Window.lua are changed
- WINDOW_H remains 536, INSET remains 6
  </acceptance_criteria>
  <done>WINDOW_W is 950 in UI/Window.lua</done>
</task>

<task type="auto">
  <name>Task 2: Update CONTENT_W, COL_X, and COL_W in UI/TabPlayers.lua</name>
  <files>UI/TabPlayers.lua</files>
  <read_first>UI/TabPlayers.lua (lines 1-30)</read_first>
  <action>
Change line 17 of UI/TabPlayers.lua:
- FROM: `local CONTENT_W      = 788   -- full tab frame width (WINDOW_W 800 - INSET*2 6)`
- TO:   `local CONTENT_W      = 938   -- full tab frame width (WINDOW_W 950 - INSET*2 6)`

Change line 26 (COL_X) from:
```lua
local COL_X = { status = 0, name = 22, level = 168, class = 218, spec = 314, time = 420, actions = 672 }
```
to:
```lua
local COL_X = { status = 0, name = 22, level = 168, class = 218, spec = 314, activity = 420, time = 576, actions = 828 }
```

Change line 27 (COL_W) from:
```lua
local COL_W = { status = 18, name = 140, level = 44, class = 90, spec = 100, time = 246, actions = 90  }
```
to:
```lua
local COL_W = { status = 18, name = 140, level = 44, class = 90, spec = 100, activity = 150, time = 246, actions = 90  }
```

Per D-02:
- activity column: COL_X.activity = 420, COL_W.activity = 150
- time column shifted: COL_X.time = 576 (was 420), COL_W.time stays 246
- actions column shifted: COL_X.actions = 828 (was 672), COL_W.actions stays 90
- All other columns (status, name, level, class, spec) are unchanged.
  </action>
  <verify>
    <automated>grep -n "CONTENT_W\|COL_X\|COL_W" UI/TabPlayers.lua | head -5</automated>
  </verify>
  <acceptance_criteria>
- UI/TabPlayers.lua contains `local CONTENT_W      = 938`
- COL_X table contains keys: status, name, level, class, spec, activity, time, actions
- COL_X.activity = 420, COL_X.time = 576, COL_X.actions = 828
- COL_W table contains `activity = 150`
- COL_W.time remains 246, COL_W.actions remains 90
  </acceptance_criteria>
  <done>CONTENT_W is 938, COL_X and COL_W include the activity column with correct positions, time and actions columns shifted right by 150px</done>
</task>

</tasks>

<verification>
- grep "WINDOW_W  = 950" UI/Window.lua returns a match
- grep "CONTENT_W.*= 938" UI/TabPlayers.lua returns a match
- grep "activity = 420" UI/TabPlayers.lua returns a match (COL_X)
- grep "activity = 150" UI/TabPlayers.lua returns a match (COL_W)
- grep "time = 576" UI/TabPlayers.lua returns a match (shifted COL_X)
- grep "actions = 828" UI/TabPlayers.lua returns a match (shifted COL_X)
</verification>

<success_criteria>
Window is 950px wide. CONTENT_W is 938. COL_X and COL_W tables contain the activity column entry. Time and actions columns are shifted right by 150px to make room.
</success_criteria>

<output>
After completion, create `.planning/phases/04-player-list-column/04-01-SUMMARY.md`
</output>
