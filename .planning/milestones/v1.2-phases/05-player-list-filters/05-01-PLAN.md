---
phase: 05-player-list-filters
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - UI/TabPlayers.lua
autonomous: true
requirements:
  - FILT-01
  - FILT-02
  - FILT-05
  - FILT-06
must_haves:
  truths:
    - "filterPrimaryActivity and filterExactActivity state variables exist as module-level locals"
    - "applyFilters logic filters entries by primaryActivity when filterPrimaryActivity is set"
    - "applyFilters logic filters entries by exactActivity when filterExactActivity is set"
    - "Both activity filters stack with existing class/spec/status/level filters via AND logic"
  artifacts:
    - path: "UI/TabPlayers.lua"
      provides: "Activity filter state variables and applyFilters extension"
      contains: "filterPrimaryActivity"
  key_links:
    - from: "filterPrimaryActivity local"
      to: "applyFilters() AND chain"
      via: "nil-guarded equality check on entry.primaryActivity"
      pattern: "filterPrimaryActivity.*entry%.primaryActivity"
    - from: "filterExactActivity local"
      to: "applyFilters() AND chain"
      via: "nil-guarded equality check on entry.exactActivity"
      pattern: "filterExactActivity.*entry%.exactActivity"
---

<objective>
Add the two activity filter state variables and extend the applyFilters() logic in TabPlayers.lua
to respect both new filters using the same AND-chain pattern as the existing four filters.

Purpose: The filtering logic must exist before the UI dropdowns or cascade wiring can function.
Output: TabPlayers.lua with filterPrimaryActivity/filterExactActivity locals and extended applyFilters().
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-player-list-filters/05-CONTEXT.md

@Data/Activities.lua
@UI/TabPlayers.lua
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add activity filter state variables and forward refs</name>
  <files>UI/TabPlayers.lua</files>
  <read_first>UI/TabPlayers.lua (lines 38-48 for existing filter state pattern)</read_first>
  <action>
Add two new module-level local variables immediately after the existing filter state block
(after `local specFilterBtn = nil` on line 47), per D-04:

```lua
local filterPrimaryActivity = nil   -- activity label string, or nil
local filterExactActivity   = nil   -- exact activity label string, or nil
local primaryActivityFilterBtn = nil  -- forward ref; set during Build
local exactActivityFilterBtn   = nil  -- forward ref; set during Build
```

These follow the exact same pattern as filterClass/filterSpec/specFilterBtn:
- nil means no filter applied
- String label value when active (e.g. "Normal Dungeon", "Karazhan")
- Forward refs for the dropdown buttons needed by Reset handler and cascade logic
  </action>
  <verify>
    <automated>grep -n "filterPrimaryActivity" UI/TabPlayers.lua | head -5</automated>
  </verify>
  <acceptance_criteria>
- UI/TabPlayers.lua contains `local filterPrimaryActivity = nil`
- UI/TabPlayers.lua contains `local filterExactActivity   = nil`
- UI/TabPlayers.lua contains `local primaryActivityFilterBtn = nil`
- UI/TabPlayers.lua contains `local exactActivityFilterBtn   = nil`
- All four new locals appear between `specFilterBtn` and the color constants (before `local WHITE`)
  </acceptance_criteria>
  <done>Four new module-level locals declared in the filter state block</done>
</task>

<task type="auto">
  <name>Task 2: Extend applyFilters() with activity filter checks</name>
  <files>UI/TabPlayers.lua</files>
  <read_first>UI/TabPlayers.lua (lines 450-472 for existing applyFilters AND chain)</read_first>
  <action>
In the `TL.Refresh()` function, inside the `for _, e in ipairs(allEntries) do` loop, add two
new filter checks after the existing `filterSpec` check (after line 470) and before the
`if ok then entries[#entries + 1] = e end` line. Per D-05, use AND logic with nil-safe guards:

```lua
        if ok and filterPrimaryActivity and e.primaryActivity ~= filterPrimaryActivity then ok = false end
        if ok and filterExactActivity and e.exactActivity ~= filterExactActivity then ok = false end
```

This mirrors the existing pattern exactly:
- `filterClass` check on line 466: `if ok and filterClass and (OW.CLASS_ID[e.class] or 0) ~= filterClass then ok = false end`
- The activity checks are simpler because they compare string labels directly (no ID lookup needed)
- `filterPrimaryActivity` matches against `entry.primaryActivity` (string field set by Phase 2)
- `filterExactActivity` matches against `entry.exactActivity` (string field set by Phase 2)
- When either filter is nil, the check is skipped (show all)
  </action>
  <verify>
    <automated>grep -n "filterPrimaryActivity\|filterExactActivity" UI/TabPlayers.lua</automated>
  </verify>
  <acceptance_criteria>
- UI/TabPlayers.lua contains `if ok and filterPrimaryActivity and e.primaryActivity ~= filterPrimaryActivity then ok = false end`
- UI/TabPlayers.lua contains `if ok and filterExactActivity and e.exactActivity ~= filterExactActivity then ok = false end`
- Both lines appear inside the `for _, e in ipairs(allEntries) do` loop in TL.Refresh()
- Both lines appear after the filterSpec check and before `if ok then entries[#entries + 1] = e end`
  </acceptance_criteria>
  <done>applyFilters AND chain includes primaryActivity and exactActivity checks; nil = no filter</done>
</task>

</tasks>

<verification>
- `grep -c "filterPrimaryActivity" UI/TabPlayers.lua` returns at least 2 (declaration + usage)
- `grep -c "filterExactActivity" UI/TabPlayers.lua` returns at least 2 (declaration + usage)
- `grep -c "primaryActivityFilterBtn" UI/TabPlayers.lua` returns at least 1 (forward ref)
- `grep -c "exactActivityFilterBtn" UI/TabPlayers.lua` returns at least 1 (forward ref)
- The file has no syntax errors (balanced `end` keywords, no stray characters)
</verification>

<success_criteria>
- filterPrimaryActivity and filterExactActivity locals exist in the filter state block
- primaryActivityFilterBtn and exactActivityFilterBtn forward refs exist
- applyFilters() respects both new filters with AND logic
- Existing filter behavior unchanged (status, level, class, spec still work)
</success_criteria>

<output>
After completion, create `.planning/phases/05-player-list-filters/05-01-SUMMARY.md`
</output>
