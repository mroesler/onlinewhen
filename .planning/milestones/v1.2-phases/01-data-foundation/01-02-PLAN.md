---
phase: 01-data-foundation
plan: 02
type: execute
wave: 2
depends_on:
  - 01-01
files_modified:
  - OnlineWhen.toc
autonomous: false

requirements:
  - DATA-01

must_haves:
  truths:
    - "Data/Activities.lua appears in OnlineWhen.toc between Data/Timezones.lua and Core/Status.lua"
    - "The addon loads without Lua errors when /ow is run in-game"
  artifacts:
    - path: "OnlineWhen.toc"
      provides: "Load-order registration for Data/Activities.lua"
      contains: "Data/Activities.lua"
  key_links:
    - from: "OnlineWhen.toc"
      to: "Data/Activities.lua"
      via: "TOC file list entry"
      pattern: "Data/Activities\\.lua"
---

<objective>
Register Data/Activities.lua in OnlineWhen.toc at the correct load-order position and verify the addon loads cleanly.

Purpose: Without the TOC entry, the WoW client never loads the file and all OW.ACTIVITY references will be nil at runtime.
Output: Updated OnlineWhen.toc with Data/Activities.lua in position.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@OnlineWhen.toc
@.planning/phases/01-data-foundation/01-01-SUMMARY.md

<interfaces>
<!-- Current TOC structure (from OnlineWhen.toc) -->
Current load order:
```
Locales/enUS.lua
Data/Timezones.lua          <-- Activities.lua goes AFTER this
Core/Status.lua             <-- Activities.lua goes BEFORE this
Core/Classes.lua
Core/Specs.lua
Core/Database.lua
Network/Protocol.lua
UI/Window.lua
UI/TabSchedule.lua
UI/TabPlayers.lua
Core/Commands.lua
Core/Init.lua
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Insert Data/Activities.lua into OnlineWhen.toc</name>
  <files>OnlineWhen.toc</files>
  <read_first>OnlineWhen.toc</read_first>
  <action>
Edit OnlineWhen.toc to add `Data/Activities.lua` on its own line immediately after `Data/Timezones.lua` and before `Core/Status.lua`.

The resulting file list section must read:
```
Locales/enUS.lua
Data/Timezones.lua
Data/Activities.lua
Core/Status.lua
Core/Classes.lua
Core/Specs.lua
Core/Database.lua
Network/Protocol.lua
UI/Window.lua
UI/TabSchedule.lua
UI/TabPlayers.lua
Core/Commands.lua
Core/Init.lua
```

Do NOT change any other lines. Do NOT change the header metadata lines (Interface, Title, Notes, Author, Version, SavedVariables).
  </action>
  <verify>
    <automated>grep -n "Data/Activities.lua" OnlineWhen.toc && grep -A1 "Data/Timezones.lua" OnlineWhen.toc | grep "Data/Activities.lua" && grep -A1 "Data/Activities.lua" OnlineWhen.toc | grep "Core/Status.lua"</automated>
  </verify>
  <acceptance_criteria>
- OnlineWhen.toc contains the line `Data/Activities.lua`
- The line immediately after `Data/Timezones.lua` is `Data/Activities.lua`
- The line immediately after `Data/Activities.lua` is `Core/Status.lua`
- No other lines were added, removed, or modified
- Header metadata (## Interface, ## Title, etc.) is unchanged
  </acceptance_criteria>
  <done>OnlineWhen.toc contains Data/Activities.lua between Data/Timezones.lua and Core/Status.lua.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Verify addon loads without errors in WoW client</name>
  <files>Data/Activities.lua, OnlineWhen.toc</files>
  <action>
Human verification checkpoint. Claude has completed all automated work for Phase 1. The user must now verify in the WoW client that the addon loads without errors and all activity data structures are accessible at runtime. Follow the steps in how-to-verify below.
  </action>
  <what-built>Data/Activities.lua — a new static data module with the OW.ACTIVITY enum (7 activities), OW.ACTIVITY_LIST (ordered display list), and OW.ACTIVITY_SUBS (sub-type tables for all TBC Classic Anniversary dungeons, raids, and battlegrounds). Registered in OnlineWhen.toc.</what-built>
  <how-to-verify>
    1. Launch WoW TBC Classic Anniversary client (or reload UI with `/reload`)
    2. Check that no red Lua error popup appears on login
    3. Type `/ow` — the OnlineWhen window should open normally
    4. In chat, run: `/run print(OW.ACTIVITY.NORMAL_DUNGEON)` — should print `1`
    5. In chat, run: `/run print(OW.ACTIVITY.CHILL)` — should print `7`
    6. In chat, run: `/run print(#OW.ACTIVITY_LIST)` — should print `7`
    7. In chat, run: `/run print(#OW.ACTIVITY_SUBS["Normal Dungeon"])` — should print `16`
    8. In chat, run: `/run print(#OW.ACTIVITY_SUBS["Heroic Dungeon"])` — should print `16`
    9. In chat, run: `/run print(#OW.ACTIVITY_SUBS["Raid"])` — should print `9`
    10. In chat, run: `/run print(#OW.ACTIVITY_SUBS["PVP"])` — should print `4`
    11. In chat, run: `/run print(OW.ACTIVITY_SUBS["Quest"] ~= nil)` — should print `true`
    12. In chat, run: `/run print(#OW.ACTIVITY_SUBS["Quest"])` — should print `0`
    13. In chat, run: `/run print(OW.ACTIVITY.FAKE)` — should produce a Lua error containing "OW.ACTIVITY: unknown key: FAKE"
  </how-to-verify>
  <verify>
    <automated>MISSING — WoW addon Lua executes inside the WoW client sandbox; verification is manual in-game only</automated>
  </verify>
  <acceptance_criteria>
- No Lua error popup on addon load
- `/ow` opens the OnlineWhen window without errors
- OW.ACTIVITY.NORMAL_DUNGEON prints 1
- OW.ACTIVITY.CHILL prints 7
- #OW.ACTIVITY_LIST prints 7
- #OW.ACTIVITY_SUBS["Normal Dungeon"] prints 16
- #OW.ACTIVITY_SUBS["Raid"] prints 9
- #OW.ACTIVITY_SUBS["PVP"] prints 4
- OW.ACTIVITY_SUBS["Quest"] is not nil and has length 0
- OW.ACTIVITY.FAKE raises a Lua error
  </acceptance_criteria>
  <done>User has confirmed all 13 verification steps pass in the WoW client. Phase 1 is complete.</done>
  <resume-signal>Type "approved" or describe any issues found</resume-signal>
</task>

</tasks>

<verification>
- `grep "Data/Activities.lua" OnlineWhen.toc` returns a match
- `grep -A1 "Data/Timezones.lua" OnlineWhen.toc` shows `Data/Activities.lua` on the next line
- In-game: `/ow` opens without errors; all `/run` checks pass
</verification>

<success_criteria>
Data/Activities.lua is registered in the TOC at the correct load-order position and the addon loads without Lua errors. All 5 ROADMAP success criteria are verified in-game.
</success_criteria>

<output>
After completion, create `.planning/phases/01-data-foundation/01-02-SUMMARY.md`
</output>
