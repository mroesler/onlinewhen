---
phase: 02-database-protocol
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - Core/Database.lua
autonomous: true
requirements:
  - NET-03
must_haves:
  truths:
    - "OW.SaveMyEntry accepts 8 parameters including primaryActivity and exactActivity"
    - "Calling SaveMyEntry with activity values stores them in OnlineWhenDB.myEntry"
    - "Calling SaveMyEntry with nil activity values results in nil fields in myEntry (not empty string)"
  artifacts:
    - path: "Core/Database.lua"
      provides: "Extended OW.SaveMyEntry with activity fields"
      contains: "function OW.SaveMyEntry(name, spec, class, level, onlineAt, tzId, primaryActivity, exactActivity)"
  key_links:
    - from: "Core/Database.lua"
      to: "OnlineWhenDB.myEntry"
      via: "struct assignment in SaveMyEntry"
      pattern: "primaryActivity.*=.*primaryActivity"
---

<objective>
Extend OW.SaveMyEntry to accept and store primaryActivity and exactActivity fields.

Purpose: NET-03 requires that SaveMyEntry accepts activity fields and persists them into OnlineWhenDB.myEntry. This is the database-side half of the activity data flow — once stored in myEntry, BroadcastSelf will automatically pick them up via SerializeANN (after Plan 02-02 extends the serializer).

Output: Modified Core/Database.lua with extended SaveMyEntry signature.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-database-protocol/02-CONTEXT.md
@.planning/phases/02-database-protocol/02-RESEARCH.md
@.planning/codebase/CONVENTIONS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend OW.SaveMyEntry signature and struct assignment</name>
  <files>Core/Database.lua</files>
  <read_first>Core/Database.lua</read_first>
  <action>
Modify `OW.SaveMyEntry` in Core/Database.lua to accept two new trailing parameters and store them in the myEntry table.

Change the function signature from:
```lua
function OW.SaveMyEntry(name, spec, class, level, onlineAt, tzId)
```
to:
```lua
function OW.SaveMyEntry(name, spec, class, level, onlineAt, tzId, primaryActivity, exactActivity)
```

Add two new fields to the `OnlineWhenDB.myEntry` table assignment, after the `updated = time()` line:
```lua
        primaryActivity = primaryActivity or nil,
        exactActivity   = exactActivity   or nil,
```

The `or nil` idiom normalizes empty string to nil, matching the existing pattern for optional fields. Per D-05, do NOT validate activity values against OW.ACTIVITY_LIST — pass through as-is.

Update the file header comment on line 3 to still list SaveMyEntry in the Provides line (no change needed if it already does — just verify).

No other functions in Database.lua need changes. UpsertPeer already stores the full entry table as-is — activity fields will flow through automatically once HandleANN includes them (Plan 02-02).
  </action>
  <verify>
    <automated>grep -n "primaryActivity" Core/Database.lua | head -10</automated>
  </verify>
  <acceptance_criteria>
    - Core/Database.lua line 20 contains `function OW.SaveMyEntry(name, spec, class, level, onlineAt, tzId, primaryActivity, exactActivity)`
    - Core/Database.lua contains `primaryActivity = primaryActivity or nil,`
    - Core/Database.lua contains `exactActivity   = exactActivity   or nil,`
    - Core/Database.lua does NOT contain `ACTIVITY_LIST` or `VALID_ACTIVITIES` (per D-05: no validation)
    - The `if OW.Protocol then OW.Protocol.BroadcastSelf() end` block remains unchanged after the table assignment
  </acceptance_criteria>
  <done>OW.SaveMyEntry accepts 8 parameters; primaryActivity and exactActivity are stored in OnlineWhenDB.myEntry; no activity validation is performed</done>
</task>

</tasks>

<verification>
- `grep "primaryActivity" Core/Database.lua` returns at least 2 lines (parameter + assignment)
- `grep "exactActivity" Core/Database.lua` returns at least 2 lines (parameter + assignment)
- `grep "VALID_ACTIVITIES\|ACTIVITY_LIST" Core/Database.lua` returns nothing (no validation per D-05)
- Function signature has exactly 8 parameters
</verification>

<success_criteria>
OW.SaveMyEntry(name, spec, class, level, onlineAt, tzId, primaryActivity, exactActivity) stores both activity fields in OnlineWhenDB.myEntry. Existing callers (currently passing 6 args) continue to work — the two new params default to nil.
</success_criteria>

<output>
After completion, create `.planning/phases/02-database-protocol/02-01-SUMMARY.md`
</output>
