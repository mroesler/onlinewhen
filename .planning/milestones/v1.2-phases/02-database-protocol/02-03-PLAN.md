---
phase: 02-database-protocol
plan: 03
type: execute
wave: 2
depends_on:
  - 02-01
  - 02-02
files_modified:
  - Core/Database.lua
  - Network/Protocol.lua
autonomous: false
requirements:
  - NET-01
  - NET-02
  - NET-03
  - NET-04
must_haves:
  truths:
    - "SaveMyEntry stores activity fields that SerializeANN picks up and transmits as a 12-field ANN"
    - "A received 12-field ANN produces a peer entry with non-nil primaryActivity and exactActivity"
    - "A received 10-field ANN produces a peer entry with nil primaryActivity and nil exactActivity"
    - "UpsertPeer stores the activity fields from HandleANN without any additional logic"
  artifacts:
    - path: "Core/Database.lua"
      provides: "SaveMyEntry with activity fields"
      contains: "primaryActivity"
    - path: "Network/Protocol.lua"
      provides: "12-field ANN wire format"
      contains: "entry.primaryActivity or"
  key_links:
    - from: "Core/Database.lua (SaveMyEntry)"
      to: "Network/Protocol.lua (SerializeANN)"
      via: "BroadcastSelf reads OnlineWhenDB.myEntry"
      pattern: "BroadcastSelf"
    - from: "Network/Protocol.lua (HandleANN)"
      to: "Core/Database.lua (UpsertPeer)"
      via: "entry table with activity fields"
      pattern: "UpsertPeer"
---

<objective>
Verify the end-to-end round-trip: SaveMyEntry stores activity -> SerializeANN transmits it -> HandleANN receives it -> UpsertPeer stores it. Confirm backward compatibility with 10-field old-client ANNs.

Purpose: Plans 02-01 and 02-02 modified separate files. This plan verifies the integration works correctly and that all 5 success criteria from ROADMAP.md Phase 2 are met. The human-verify checkpoint ensures the data flows correctly in the WoW client.

Output: Verified round-trip; all Phase 2 success criteria confirmed.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-database-protocol/02-CONTEXT.md
@.planning/phases/02-database-protocol/02-RESEARCH.md
@.planning/phases/02-database-protocol/02-01-SUMMARY.md
@.planning/phases/02-database-protocol/02-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Code-level round-trip verification</name>
  <files>Core/Database.lua, Network/Protocol.lua</files>
  <read_first>Core/Database.lua, Network/Protocol.lua</read_first>
  <action>
Verify the data flow by reading both files and confirming:

1. **SaveMyEntry -> myEntry**: The `primaryActivity` and `exactActivity` params are stored in `OnlineWhenDB.myEntry` (Plan 02-01).

2. **BroadcastSelf -> SerializeANN**: `P.BroadcastSelf()` calls `P.SerializeANN(myEntry)`. Confirm SerializeANN includes `entry.primaryActivity or ""` and `entry.exactActivity or ""` as fields 11 and 12 (Plan 02-02).

3. **Deserialize -> split**: Confirm split uses `(str .. sep):gmatch("([^" .. sep .. "]*)" .. sep)` so empty activity tokens are preserved (Plan 02-02).

4. **validateANN**: Confirm `#fields ~= 10 and #fields ~= 12` allows both old and new clients (Plan 02-02).

5. **HandleANN -> entry -> UpsertPeer**: Confirm HandleANN constructs entry with:
   - `primaryActivity = (fields[11] and fields[11] ~= "") and fields[11] or nil`
   - `exactActivity = (fields[12] and fields[12] ~= "") and fields[12] or nil`
   And that `UpsertPeer(key, entry)` stores it as-is (no changes needed to UpsertPeer — it already stores the full entry table).

6. **Backward compat trace**: For a 10-field ANN:
   - split produces 10 entries
   - validateANN passes (`#fields == 10`)
   - HandleANN: `fields[11]` is nil -> `(nil and ...)` short-circuits to nil -> `primaryActivity = nil`
   - `fields[12]` is nil -> same -> `exactActivity = nil`
   - UpsertPeer stores entry with nil activity fields — correct

If any of these checks fail, fix the issue in the relevant file. If all checks pass, no code changes are needed — this task is verification-only.
  </action>
  <verify>
    <automated>grep -c "primaryActivity" Core/Database.lua && grep -c "primaryActivity" Network/Protocol.lua && grep -c "fields\[11\]" Network/Protocol.lua && grep "#fields ~= 10 and" Network/Protocol.lua</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "primaryActivity" Core/Database.lua` returns at least 2
    - `grep -c "primaryActivity" Network/Protocol.lua` returns at least 2
    - `grep -c "fields[11]" Network/Protocol.lua` returns at least 1
    - `grep "#fields ~= 10 and" Network/Protocol.lua` returns the line `if #fields ~= 10 and #fields ~= 12 then return false end`
    - No VALID_ACTIVITIES or ACTIVITY_LIST references in either file (per D-05)
  </acceptance_criteria>
  <done>Code-level trace confirms all 5 ROADMAP success criteria are satisfied by the combined changes from Plans 02-01 and 02-02</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: In-client smoke test</name>
  <files>Core/Database.lua, Network/Protocol.lua</files>
  <action>
Human verifies the activity field round-trip works in the WoW client. No code changes — this is a verification checkpoint after Plans 02-01 and 02-02 are complete.
  </action>
  <verify>User confirms smoke test passed by typing "approved"</verify>
  <acceptance_criteria>
    - No Lua errors when loading the addon with `/reload`
    - `/dump OnlineWhenDB.myEntry` shows a valid entry table after saving
    - `/dump OW.Protocol.SerializeANN(OnlineWhenDB.myEntry)` produces a 12-field semicolon string
    - Activity fields are nil in myEntry (expected — Phase 3 adds the UI to set them)
  </acceptance_criteria>
  <done>In-client smoke test passed; Phase 2 integration verified</done>
  <how-to-verify>
1. Log into WoW TBC Classic with the updated addon.
2. Open the OnlineWhen Schedule tab and save an entry (Phase 3 will add the activity UI — for now, activity fields will be nil; that is expected).
3. Run: `/dump OnlineWhenDB.myEntry` — verify the entry table exists with name, realm, spec, class, level, onlineAt, timezone, updated fields. primaryActivity and exactActivity will be nil since the UI does not yet pass them.
4. Run: `/dump OW.Protocol.SerializeANN(OnlineWhenDB.myEntry)` — verify it produces a 12-field semicolon string. The last two fields should be empty (two trailing semicolons visible in the output).
5. If a second client is available: have it send an ANN and inspect the received peer entry for activity fields.
6. Confirm no Lua errors appear in the chat or error log.
  </how-to-verify>
  <resume-signal>Type "approved" to confirm the smoke test passed, or describe any issues found.</resume-signal>
</task>

</tasks>

<verification>
All 5 ROADMAP Phase 2 success criteria verified:
- SC1: SaveMyEntry accepts primaryActivity and exactActivity and stores them in myEntry
- SC2: SerializeANN produces 12-field string ending in ;primaryActivity;exactActivity
- SC3: New-client ANN round-trip preserves activity fields
- SC4: Old-client 10-field ANN accepted with nil activity fields
- SC5: UpsertPeer stores activity fields from HandleANN-built entry
</verification>

<success_criteria>
1. Code-level trace confirms end-to-end data flow for activity fields.
2. In-client smoke test shows no Lua errors and 12-field ANN output.
3. All Phase 2 ROADMAP success criteria are met.
</success_criteria>

<output>
After completion, create `.planning/phases/02-database-protocol/02-03-SUMMARY.md`
</output>
